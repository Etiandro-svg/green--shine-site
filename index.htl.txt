<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Green Shine - Limpeza Profissional</title>
  <style>
    /* ===== Geral ===== */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      background: #f2f2f2;
      color: #333;
      scroll-behavior: smooth;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* ===== Header com gradiente animado ===== */
    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    header {
      background: linear-gradient(-45deg, #28a745, #85e085, #0a5500, #a0ff9e);
      background-size: 400% 400%;
      animation: gradientMove 15s ease infinite;
      color: white;
      text-align: center;
      padding: 25px 10px;
      position: relative;
      overflow: hidden;
    }

    .logo {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .logo h1 {
      margin: 10px 0 0 0;
      font-size: 2.7em;
      color: #ffffff;
      text-shadow: 1px 1px 3px rgba(0,0,0,0.3);
    }

    /* ===== Logotipo animado ===== */
    .logo svg {
      width: 110px;
      height: 110px;
    }

    .pano-amarelo {
      transform-origin: center;
      animation: balanço 2s ease-in-out infinite alternate;
    }

    .pano-azul {
      transform-origin: center;
      animation: balanço 2.5s ease-in-out infinite alternate-reverse;
    }

    @keyframes balanço {
      0% { transform: rotate(-10deg); }
      50% { transform: rotate(10deg); }
      100% { transform: rotate(-10deg); }
    }

    nav {
      margin-top: 15px;
    }

    nav a {
      color: white;
      font-weight: bold;
      margin: 0 18px;
      font-size: 1.2em;
      padding: 5px 8px;
      border-radius: 5px;
      transition: background 0.3s, transform 0.3s;
    }

    nav a:hover {
      background: rgba(255,255,255,0.2);
      transform: scale(1.1);
    }

    /* ===== Main ===== */
    main {
      padding: 30px 20px;
      max-width: 1100px;
      margin: auto;
    }

    h2 {
      color: #28a745;
      text-align: center;
      font-size: 2em;
      margin-bottom: 25px;
    }

    /* ===== Serviços com micro-interações ===== */
    .services {
      display: flex;
      flex-wrap: wrap;
      gap: 25px;
      justify-content: center;
    }

    .service {
      background: #ffffffcc;
      flex: 1 1 280px;
      padding: 25px;
      border-radius: 20px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.1);
      transition: transform 0.4s, box-shadow 0.4s, background 0.4s;
      cursor: pointer;
    }

    .service:hover {
      transform: translateY(-10px) scale(1.05);
      box-shadow: 0 15px 30px rgba(0,0,0,0.2);
      background: #e6ffe6;
    }

    .service h3 {
      margin-top: 0;
      color: #0a5500;
      font-size: 1.4em;
    }

    /* ===== Contacto ===== */
    .contact {
      background-color: #e0ffe0;
      padding: 30px;
      border-radius: 20px;
      margin-top: 40px;
      text-align: center;
      box-shadow: 0 6px 15px rgba(0,0,0,0.1);
    }

    .contact p {
      font-size: 1.25em;
      margin: 12px 0;
    }

    /* ===== Footer ===== */
    footer {
      text-align: center;
      padding: 20px;
      background-color: #28a745;
      color: white;
      margin-top: 40px;
    }

    /* ===== Botão WhatsApp flutuante com pulse ===== */
    .whatsapp-button {
      position: fixed;
      bottom: 25px;
      right: 25px;
      background-color: #25d366;
      color: white;
      border-radius: 50%;
      width: 65px;
      height: 65px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 32px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.25);
      z-index: 1000;
      cursor: pointer;
      animation: pulse 2s infinite;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .whatsapp-button:hover {
      transform: scale(1.15);
      box-shadow: 0 10px 25px rgba(0,0,0,0.3);
    }

    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.15); }
      100% { transform: scale(1); }
    }

    /* ===== Responsividade ===== */
    @media (max-width: 768px) {
      nav a {
        display: block;
        margin: 10px 0;
      }
    }

  </style>
</head>
<body>

  <header>
    <div class="logo">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
        <!-- Borrifador -->
        <rect x="28" y="10" width="8" height="20" fill="#0a5500"/>
        <rect x="25" y="30" width="14" height="24" fill="#28a745"/>
        <!-- Panos animados -->
        <circle class="pano-amarelo" cx="18" cy="50" r="6" fill="#FFD700"/>
        <circle class="pano-azul" cx="46" cy="50" r="6" fill="#1E90FF"/>
      </svg>
      <h1>Green Shine</h1>
    </div>
    <nav>
      <a href="#services">Serviços</a>
      <a href="#contact">Contacto</a>
    </nav>
  </header>

  <main>
    <section id="services">
      <h2>Nossos Serviços</h2>
      <div class="services">
        <div class="service">
          <h3>Limpeza de Casas de Férias</h3>
          <p>Garantimos que a sua casa de férias esteja sempre limpa e pronta para receber visitas.</p>
        </div>
        <div class="service">
          <h3>Escritórios</h3>
          <p>Limpeza profissional de escritórios e espaços de trabalho, mantendo o ambiente saudável.</p>
        </div>
        <div class="service">
          <h3>Limpeza Pós-Obra</h3>
          <p>Remoção de pó e resíduos após construções ou reformas.</p>
        </div>
        <div class="service">
          <h3>Prédios e Condomínios</h3>
          <p>Serviço completo para áreas comuns de prédios, garantindo limpeza e higiene.</p>
        </div>
      </div>
    </section>

    <section id="contact" class="contact">
      <h2>Contacto</h2>
      <p>📞 Telemóvel / WhatsApp: <a href="https://wa.me/351939329061" target="_blank">939 329 061</a></p>
      <p>📍 Localização: Grande Lisboa</p>
      <p>Sem horários fixos – estamos disponíveis conforme a sua necessidade!</p>
    </section>
  </main>

  <footer>
    &copy; 2025 Green Shine. Todos os direitos reservados.
  </footer>

  <!-- Botão WhatsApp flutuante -->
  <a href="https://wa.me/351939329061" target="_blank" class="whatsapp-button">💬</a>

</body>
</html>
